{
  "name": "@evm-transaction-trace/examples",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "check": "tsc --pretty --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules",
    "test": "vitest run",
    "test:watch": "vitest run",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5",
    "@evm-transaction-trace/typescript-config": "workspace:*"
  },
  "dependencies": {
    "viem": "^2.33.3",
     "@evm-transaction-trace/tracer": "workspace:*"
  }
}
