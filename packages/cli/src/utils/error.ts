export enum EVM_TT_ERRORS {
  RPC = 'No RPC url provided. used the flag --rpc, or add one using the cli',
  ChainId = 'No chainId provided. used the flag --chain-id, or add one using the cli',
  EtherscanKey = 'No api key provided. used the flag --etherscan-key, or add one using the cli',
}
